"""Agents in the Wild — deliberately-naive multi-tenant multi-agent target system.

See README.md and docs/superpowers/specs/ for the two governing invariants:
naive-on-purpose, and nothing-leaves-the-box. Do not "harden" the deliberate
weaknesses (see aitw.context.store).
"""

__version__ = "0.1.0"
